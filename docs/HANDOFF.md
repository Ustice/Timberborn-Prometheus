# Prometheus Handoff

## Current Focus

Last updated: 2026-04-28

Prometheus is moving into Phase 3: intentional fire and ash harvest. The active sprint should make controlled burns an emergent player strategy, not a new simulation mechanic. Players prepare containment with terrain, moisture, water, spacing, barriers, and firebreaks, then intentionally ignite one selected valid target. The economy milestone for this phase is visible Fertile Ash harvest and storage from burned trees, crops, and buildings; farmhouse ash application remains deferred.

Phase 2 stabilization closeout is complete for the integrated scope. P2S-001 through P2S-024, P2S-026, and P2S-027 are integrated or closeout-verified. P2S-025 is explicitly pushed out and remains blocked on live farmhouse amendment evidence; its compile-clean prototype was discarded so the next pass can be redesigned from the P2S-024 discovery notes. The closed sprint ticket board is archived under [ARCHIVE/stabilization-sprint-2026-04/](ARCHIVE/stabilization-sprint-2026-04/), and ongoing ticket tracking now lives under [tickets/README.md](tickets/README.md). Active Phase 3 implementation tickets are [tickets/verify/TKT-003-ignite-selected-target.md](tickets/verify/TKT-003-ignite-selected-target.md), [tickets/in-progress/TKT-004-fertile-ash-from-burned-crops.md](tickets/in-progress/TKT-004-fertile-ash-from-burned-crops.md), [tickets/in-progress/TKT-005-containment-validation-matrix.md](tickets/in-progress/TKT-005-containment-validation-matrix.md), and [tickets/in-progress/TKT-006-runtime-visual-readability.md](tickets/in-progress/TKT-006-runtime-visual-readability.md). The deferred farmhouse work remains [tickets/deferred/TKT-001-farmhouse-fertile-ash-application.md](tickets/deferred/TKT-001-farmhouse-fertile-ash-application.md).

## Verified Since Last Checkpoint

| Date | Command / Evidence | Result | Notes |
| --- | --- | --- | --- |
| 2026-04-28 | User live visual feedback | Follow-up Patch | User confirmed the `_Grayscale` / `_LightingStrength` path visibly turns pines gray, but the result is not dark enough. Follow-up patch keeps grayscale staging and lowers `_LightingStrength` more aggressively: dried is `0.45`, dried-and-charred is `0`, dead-and-charred is `-0.35`, and stump-and-charred is `-0.75` if the shader accepts negative values. |
| 2026-04-28 | `Fire.log` shader-property scan + `bash scripts/build.sh --test --launch` + startup log scan | Follow-up Patch | The shader-property probe found pine materials expose `_Grayscale`, `_LightingStrength`, `_MainTex=Pine_D`, and `_BumpMap=Pine_N`; `_Color` remained visible in telemetry but is not useful in practice. Follow-up patch applies a reversible material-instance char effect for tree `VegetationURP` materials by progressively raising `_Grayscale` and lowering `_LightingStrength` across dried, dried-and-charred, dead-and-charred, and stump-and-charred stages. It logs `visual_surface_material_char_applied` when stages change. Preflight passed with `107` tests, deploy completed, logs were cleared, Timberborn relaunched, and Prometheus v0.2 loaded. |
| 2026-04-28 | User live visual feedback + `bash scripts/build.sh --test --launch` + startup log scan | Diagnostic Build | User reported the material `_Color` tint did not visibly change pines even though prior logs showed `visual_surface_material_tint_applied` firing. Follow-up patch removes the failed runtime tint mutation and replaces it with passive tree material/shader discovery: `visual_surface_material_discovered` now logs shader property names, property types, values, texture slots, texture names/sizes, and render queue once a tree reaches desiccation/char. Preflight passed with `107` tests, deploy completed, logs were cleared, Timberborn relaunched, Prometheus v0.2 loaded, and the current QA save reached post-load. |
| 2026-04-28 | User live visual feedback + `bash scripts/build.sh --test --launch` + startup log scan | Follow-up Patch | User reported the highlighter-based char attempt made trees lighter and applied immediately when trees ignited. Follow-up patch disables runtime surface tint entirely for now; tree visual readability is back to native `#Dying`, `#Dead`, `#Leftover` model states plus fire/smoke/steam particles. Preflight passed with `107` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, the scanned logs had no `visual_surface_tint*` rows, and `git diff --check` was clean. |
| 2026-04-28 | Hue Turn X source inspection + `bash scripts/test.sh` + `bash scripts/build.sh --launch` + startup log scan | Follow-up Patch | Hue Turn X colors plants by adding a component to `BlockObject`s and using Timberborn `Highlighter.HighlightPrimary(...)`; transparency is separate and uses its `TransparentShaders` dependency. Prometheus char/desiccation tint now prefers the same `Timberborn.SelectionSystem.Highlighter` path through a reflection adapter, with the old renderer material/property-block tint retained as fallback. Plain tests passed with `107` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, `git diff --check` was clean, and the startup scan had no highlighter failure telemetry. |
| 2026-04-28 | User visual rule request + `bash scripts/build.sh --test --launch` + startup log scan | Follow-up Patch | User clarified the target progression: fire should go out when a tree reaches the dead state, smoke should remain through the dead state, and stump smoke should fade quickly. Follow-up patch removes dead-tree flame entirely, raises dead-tree smoke while fuel remains below the stump threshold, and fades stump smoke out by roughly `0.61` fuel consumed. Preflight passed with `107` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, and `git diff --check` was clean. |
| 2026-04-28 | User live visual feedback + `bash scripts/build.sh --test --launch` + startup log scan | Follow-up Patch | User reported the flame still sticks around too long once the tree becomes a stump. Follow-up patch hard-stops dead-tree flame once exposure crosses the stump visual threshold (`0.45` fuel consumed), leaving any remaining post-stump burn as char/smoke rather than visible flame. Preflight passed with `107` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, and the scanned logs had no startup exception. |
| 2026-04-28 | User live visual feedback + `bash scripts/build.sh --test --launch` + startup log scan | Follow-up Patch | User confirmed pines now reach visible stumps, but the fire sticks around too long after the tree is burned down. Follow-up patch tunes only the visual flame tail: dead tree flame is reduced and fades out between roughly `0.45` and `0.72` fuel consumed, while underlying fuel/burn physics remain unchanged. Preflight passed with `107` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, and the scanned logs had no startup exception. |
| 2026-04-28 | User live visual feedback + log scan + `bash scripts/build.sh --test --launch` + startup log scan | Follow-up Patch | User still did not see pine stumps after the earlier threshold change. Logs showed live pines reaching and passing the new stump visual threshold (`fuel` around `0.45`), so the issue was in model visibility rather than burn duration. Follow-up patch treats `#Leftover` as an age-local native model child when it lives under `Seedling` or `Mature`, instead of activating it and then hiding its parent age root. Dead-but-still-burning trees now keep reduced flame visuals; dead buildings still do not. Preflight passed with `107` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, and `git diff --check` was clean. |
| 2026-04-28 | User live visual feedback + `bash scripts/build.sh --test --launch` + startup log scan | Follow-up Patch | User confirmed native pine model-state switching is now visible, but reported the visible fire went out before the tree reached dead or stump. Follow-up patch keeps spread physics unchanged and moves tree visual progression earlier: `#Dead` can begin from accumulated fuel consumed at `0.18`, and `#Leftover` can begin at `0.45` or burned-out exposure. Preflight passed with `106` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, and the scanned logs had no Prometheus exception. |
| 2026-04-28 | User live visual feedback + `bash scripts/build.sh --test --launch` + startup log scan | Follow-up Patch | User confirmed the rejected generated overlay blocks are gone, then asked whether trees can have additional states. Pine blueprints already include native `#Alive`, `#Dying`, `#Dead`, and `#Leftover` model children. Follow-up patch adds a Prometheus tree model-state switcher in `FireDamageEffectApplier`: healthy maps to `#Alive`, dried/dried-and-charred to `#Dying`, dead-and-charred to `#Dead`, and stump-and-charred to `#Leftover`. Preflight passed with `106` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, and the scanned logs had no Prometheus exception. |
| 2026-04-28 | User visual-direction request + `bash scripts/build.sh --test --launch` + startup log scan | Follow-up Patch | Tree visual progression now has an explicit natural-resource stage sequence: healthy, dried, dried-and-charred, dead-and-charred, then stump-and-charred only after burnout. `FireDamageEffectApplier` keeps live tree resources in Timberborn's dried/dead-looking state while they are dead-but-not-burned-out, and only sets the native stump/dead flag once fuel is burned out. Preflight passed with `105` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, and the scanned logs had no Prometheus exception. |
| 2026-04-28 | User live QA feedback + `bash scripts/build.sh --test --launch` | Follow-up Patch | User reported crops are good enough for now, but trees still only maybe light one other tree. Follow-up patch leaves crop tuning unchanged and adds a weak tree-only horizontal halo with radius `2`; immediate-ring falloff is `0.18` so first-hop speed should stay close to the accepted feel, while outer-ring falloff is `0.32` to preheat the next tree ring and help the chain travel farther. Preflight passed with `104` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, and `git diff --check` was clean. |
| 2026-04-28 | User live QA feedback + `bash scripts/build.sh --test --launch` | Follow-up Patch | User reported tree spread speed feels good but does not travel far enough, likely because trees lose heat too fast; crop radius-`2` sparks are visible but do not ignite. Follow-up patch adds a tree-only fuel consumption multiplier of `0.75` so trees emit strong heat longer without increasing first-hop speed, and raises crop outer-ring halo strength from `0.28` to `0.38` while keeping immediate crop strength at `0.68`. Preflight passed with `104` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, and `git diff --check` was clean. |
| 2026-04-28 | User live QA feedback + log scan + `bash scripts/build.sh --test --launch` | Follow-up Patch | User reported crops still spread fast, trees do not spread fast enough, and Timberborn hung after reset plus relighting. Logs showed reset completed in ~70ms with `failures=0`, the grid stayed bounded, but Timberborn later pegged CPU while active fires were running. Code review found `FireBeaverEffectApplier` rescanned all component caches from every fire-profiled entity whenever the NeedManager API was missing because the cached manager list stayed empty. Patch throttles that scan to the shared 5s interval, raises global neighbor heat/ember weights to `0.15`/`0.12` for trees, and trims crop halo strengths to `0.68` immediate / `0.28` outer to offset the global bump. Preflight passed with `104` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, and `git diff --check` was clean. |
| 2026-04-28 | User live QA feedback + `bash scripts/build.sh --test --launch` | Follow-up Patch | User reported radius-`1` still only lights the initial neighbors. Follow-up patch restores crop halo radius `2` but trims it below the fast-spread pass: immediate neighbor strength is `0.72` and outer-ring strength is `0.32`, while crop fuel consumption remains `1.6`. This should restore second-ring preheat without the earlier radius-`2` leapfrog. Preflight passed with `104` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, and `git diff --check` was clean. |
| 2026-04-28 | User live QA feedback + `bash scripts/build.sh --test --launch` | Follow-up Patch | User reported the radius-`1`, strength-`0.70` crop halo made carrots go out again. Follow-up patch keeps crop spread radius at `1`, restores immediate-neighbor strength to `0.78`, and slows crop fuel consumption from `1.8` to `1.6` so contiguous crops have enough burn duration to hand off without reintroducing radius-`2` leapfrogging. Preflight passed with `104` tests, deploy completed, logs were cleared, Timberborn relaunched, startup showed Prometheus v0.2 loaded, and `git diff --check` was clean. |
| 2026-04-28 | User live QA feedback | Follow-up Patch | User confirmed crops are in a contiguous block and the crop fire now spreads through the crop with no smoke wave, but the spread is slightly too fast. Follow-up patch narrows the crop-only halo from radius `2` to radius `1` and trims immediate-neighbor strength from `0.78` to `0.70`, keeping crop chaining local without two-cell leapfrogging. |
| 2026-04-28 | User live QA feedback | Follow-up Patch | User confirmed the smoke wave is gone, but crop propagation now stops after the initial ignition and a few nearby carrots. Follow-up patch adds a crop-only horizontal near-field heat/ember halo with radius `2`, injected only into existing non-air, non-underwater grid environments, and blocks air cells from both receiving and relaying non-self propagation. This should let newly ignited carrots hand fire to nearby crop cells without restoring the far-reaching smoke wave. |
| 2026-04-28 | User live QA + instrumented `Fire.log` scan | Follow-up Patch | User still saw the crop smoke wave and chug after four carrots burned in a square. Runtime telemetry found two concrete causes: entities with no exposure rendered smoke because the visual fallback used the terminal dead-building snapshot, and the fire grid expanded from `2` active cells to `182475` active cells after the carrot ignition. Follow-up patch changes no-exposure visuals to a cold snapshot and prevents non-self grid transfer into open air, so empty air cells no longer relay heat/smoke/embers across the map. |
| 2026-04-28 | User live QA + `Fire.log` / `Player.log` scan | Instrumented | User reported the same smoke wave, but horizontal carrot ignition improved and four carrots burned in a square. Logs again show low event volume (`38` `burning_tick` rows, no per-second spike) and four carrot ignitions. Added temporary runtime telemetry: `grid_runtime_state` logs active grid cells/chunks once per second, and `visual_runtime_intensity` logs nonzero visual channels with entity/profile/category context under a capped budget. Use this to identify whether the remaining wave is grid expansion, embers/steam, or non-crop smoke visuals. |
| 2026-04-28 | User live QA + `Fire.log` / `Player.log` scan | Follow-up Patch | User still saw the smoke wave and slowdown, though horizontal carrot ignition improved. Log counts do not support logging as the slowdown cause: only `57` `burning_tick` rows total and no per-second total above `10`. Follow-up patch suppresses crop smoke directly in `FireVisualEffectApplier` from the crop `FireProfile`, avoiding the earlier dependency on damage-state category timing during early ignition. |
| 2026-04-28 | User live QA + `Fire.log` / `Player.log` scan | Follow-up Patch | User reported the same smoke wave, same vertical spread pattern, and same slowdown after the smoke-only active-cell fix. Logs again showed crop ignition rows but no captured tree ignition before Timberborn was stopped. Follow-up patch treats crop footprints as one transform cell instead of renderer bounds, because crop renderer bounds can make a burning crop sample a line-shaped footprint; crop smoke visuals are disabled so burning crops do not create native smoke plumes. |
| 2026-04-28 | User live QA + `Fire.log` / `Player.log` scan | Follow-up Patch | User still saw a far-reaching smoke wave into distant trees, followed by simulation slowdown. Logs showed crop burn telemetry but did not show tree ignition in the capture, pointing to smoke-field visualization and smoke-only active grid cells. Follow-up patch makes smoke decay faster, reduces neighbor/upward smoke propagation, removes healthy non-burning smoke visuals, and prevents smoke-only cells from keeping the fire grid active. Timberborn was stopped before redeploy to end the slow sim. |
| 2026-04-28 | User live QA + `Fire.log` / `Player.log` scan | Follow-up Patch | User reported a fast initial smoke wave across the field, with only plants in one row sustaining fire while the rest went out almost as fast as they lit. Logs showed real crop ignitions and crop ash, but the broad wave was consistent with non-burning smoke exposure being rendered too visibly. Follow-up patch suppresses low non-burning smoke visuals, lowers carrot ignition threshold, shifts crop burning output toward ember instead of smoke, and slows crop fuel consumption slightly. |
| 2026-04-28 | User live QA feedback | Follow-up Patch | User reported crop fires looked directionally biased, spreading vertically from the current viewpoint instead of horizontally. Code review found the central `FireGridSimulationSingleton` existed but was not registered, while each `FireExposureController` could trigger the grid step from entity update order. Follow-up patch registers the singleton and removes per-entity grid stepping so the shared fire grid advances centrally once per frame. |
| 2026-04-28 | User live QA feedback | Follow-up Patch | User reported crops appeared to affect trees much farther away. Follow-up patch dampens crop-profile burning heat, ember, and smoke emissions while lowering carrot ignition threshold to keep crop-to-crop spread viable. |
| 2026-04-28 | User live QA + `Fire.log` / `Player.log` scan | Follow-up Patch | User reported tuned trees worked well, but crops did not spread well and the game hung soon after igniting carrots. Logs showed the carrot ignition succeeded, but only one carrot entity appeared, no crop ash was reached before the log ended, and the prior tree burn still had heavy activity (`97` ignitions, `1763` burning ticks). Timberborn was stopped after capture. Follow-up patch lowers carrot fuel and ignition threshold, and makes crop-profile entities dry and consume fuel faster once lit. |
| 2026-04-28 | `bash scripts/test.sh` | Dependency-Light Pass | Fire spread tuning pass raises neighbor heat/ember transfer, active burning emission, and softens marginal ignition probability so fires should spread less timidly. Exposed non-burning smoke pressure now renders light smoke for readability. Plain C# suite is now 99 passing tests. Live spread QA is required to confirm the feel and containment balance. |
| 2026-04-27 | User live QA + `Player.log` / `Fire.log` scan | Partial Live Pass | TKT-003 selected ignition is ready for verify: tree and carrot valid-target ignition work, and logs captured invalid-target rejection for `WeatherStation.Folktails(Clone)` with `ignite_selected_rejected`. Carrot crop ash logs include `sourceKind=charredcrop`, `damageCategory=crop`, and `cropContext=burned_crop`; user live QA confirmed District 1 storage reached `Fertile ash 18`. TKT-004 remains short of verify because the user did not see ash goods on the ground despite queue/spawn/storage evidence. No Prometheus exception was scanned; one de-duplicated soil-moisture sample warning remains. |
| 2026-04-27 | `bash scripts/test.sh` | Dependency-Light Pass | TKT-006 maps grid/exposure ember pressure into restrained ember-field visuals while keeping local sparks disabled for actively burning or dead objects. Smoke, fire, steam, char, and desiccation remain driven by existing projection state. Plain C# suite is now 96 passing tests. Live prepared-burn visual QA remains required. |
| 2026-04-27 | `git diff --check` | Pass | Rewrote [ARCHITECTURE.md](ARCHITECTURE.md) as a durable target-system architecture instead of a stabilization-sprint map. Runtime verification skipped because this was documentation-only. |
| 2026-04-27 | `bash scripts/test.sh` | Pass | TKT-005 dependency-light containment matrix is covered with 96 passing plain C# tests total. Moisture, water firebreak planes, barriers, exposed-face limits, spacing, and profile thresholds now have deterministic prepared-vs-control coverage. Live containment QA still needs Computer Use evidence after the selected-target and crop-ash dependencies are ready. |
| 2026-04-27 | `git diff --check` + `bash scripts/test.sh` + `bash scripts/build.sh --qa` + Computer Use save load + log scan | Pass | Added a Prometheus world-load readiness gate backed by Timberborn `ILoadableSingleton`, `IPostLoadableSingleton`, and `IUnloadableSingleton`. Expensive component-cache scans, category detection, recovery binding, exposure ticks, workplace/beaver effects, and visual renderer/particle setup now wait until post-load. `Prometheus QA - 2026-04-27 15h28m, Day 4-11.autosave` loaded in `12644ms`; logs recorded `world_load_state_changed ready=false stage=load`, `world_load_state_changed ready=true stage=post_load`, cache resolution through `ComponentCache._components`, native visual resolution, and no scanned Prometheus exceptions. Plain C# suite is now 91 passing tests. |
| 2026-04-27 | User live QA + source fix | Active | Tree `Ignite Selected` works in live QA. Carrot crop selection reported `Selected target has no fire profile`, so carrot was missing Prometheus crop fire-profile authoring. Added a `NaturalResources/Crops/Carrot/Carrot.blueprint.json` overlay with `FireProfileSpec`; rebuild/reload before retesting carrot ignition and crop ash recovery. |
| 2026-04-27 | `git diff --check` | Pass | Created the Phase 3 intentional fire and ash harvest sprint docs/tickets, documented controlled burns as emergent containment, and added Phase 3 validation gates. Runtime verification skipped because this was documentation-only. |
| 2026-04-27 | `git diff --check` | Pass | Cleaned top-level `docs/` by moving the closed stabilization sprint plan and sprint process notes into [ARCHIVE/stabilization-sprint-2026-04/](ARCHIVE/stabilization-sprint-2026-04/), updating active links, and adding a README note to check `docs/ARCHIVE/` for retired docs. Runtime verification skipped because this was documentation-only. |
| 2026-04-27 | `git diff --check` | Pass | Added [ORCHESTRATION.md](ORCHESTRATION.md) as the durable kickoff/runbook for future multi-agent orchestration runs and linked it from README, INDEX, and the ticket board. Runtime verification skipped because this was documentation-only. |
| 2026-04-27 | Timberborn stop, `bash -n scripts/build.sh`, `bash scripts/build.sh --help`, synthetic shared-lock wait/release tests, `git diff --check`, `bash scripts/test.sh` | Pass | Added shared build/QA locking to `scripts/build.sh`. Normal build runs release the lock on exit; `--qa` keeps a persistent QA-session lock until `bash scripts/build.sh --release-qa-lock` is run. Plain C# suite stayed at 89 passing tests. |
| 2026-04-27 | Example food-chain cleanup: reference scan, JSON parse scan, `bash scripts/test.sh`, `bash scripts/build.sh` | Pass | Removed leftover example goods, need, recipes, icons, dedicated stove building registration/assets, localization rows, and bakery recipe append. Plain C# suite passed 89 tests and build/deploy completed. |
| 2026-04-25 | `bash scripts/test.sh && bash scripts/build.sh --launch` | Pass | Removal pass launched Timberborn successfully. |
| 2026-04-25 | Source inspection and build | Pass | Direct spread registry, spread ignition queue, dispatch scoring store, water context probe/store, legacy suppression applier/store, response-state labels, and floating `FIRE`/`DEAD` markers are out of active source. |
| 2026-04-25 | Blueprint update | Pass | Blueprint components now use neutral `FireProfileSpec` data. |
| 2026-04-25 | Runtime bridge | Pass | Exposure controller projects grid activity into debug, damage, recovery, and visual snapshots while grid state becomes the source of truth. |
| 2026-04-25 | `bash scripts/test.sh` | Pass | Grid foundation tests increased the plain C# suite to 21 tests. |
| 2026-04-25 | `bash scripts/build.sh --launch` + startup log scan | Pass | Debug ignition now seeds grid state; startup logs showed `Prometheus (v0.2)` and no Prometheus errors in the scanned window. |
| 2026-04-25 | `bash scripts/test.sh` | Pass | Footprint sampling and aggregate grid reads increased the plain C# suite to 23 tests. |
| 2026-04-25 | `bash scripts/build.sh --launch` + startup log scan | Pass | Entity snapshots now sample grid state across renderer-derived footprints; startup logs remained clean in the scanned window. |
| 2026-04-25 | `bash scripts/test.sh` | Pass | Environment-rule coverage increased the plain C# suite to 26 tests: underwater ignition blocking, moisture/barrier dampening, and oxygen-driven ignition differences. |
| 2026-04-25 | `bash scripts/test.sh && bash scripts/build.sh --launch` + startup log scan | Pass | Script source is organized by feature area under `Scripts/Core`, `Scripts/Debug`, and `Scripts/Fire`; startup logs showed `Prometheus (v0.2)` with no scanned Prometheus errors. |
| 2026-04-25 | `bash scripts/test.sh && bash scripts/build.sh --launch` + startup log scan | Pass | Removed leftover response-profile filenames/helper names and unused debug snapshot factory; startup logs showed `Prometheus (v0.2)` with no scanned Prometheus errors. |
| 2026-04-25 | `bash scripts/test.sh && bash scripts/build.sh --launch` + startup log scan | Pass | Renamed the grid projection bridge to exposure and changed workplace operation logs to disabled/restored; startup logs showed `Prometheus (v0.2)` with no scanned Prometheus errors. |
| 2026-04-25 | `bash scripts/test.sh && bash scripts/build.sh --launch` + startup log scan | Pass | Renamed stale `Scripts/Fire/Simulation` folder to `Scripts/Fire/Profiles`; startup logs showed `Prometheus (v0.2)` with no scanned Prometheus errors. |
| 2026-04-25 | `bash -n scripts/build.sh && bash scripts/build.sh --help && bash scripts/test.sh` | Pass | Added `--test` and `--qa` startup workflows; plain C# suite remains at 26 passing tests. |
| 2026-04-25 | `bash scripts/build.sh --qa` | Superseded | The older QA workflow waited for Prometheus startup readiness after launch. That wait has been removed; `--qa` now exits after Timberborn is launched so Computer Use can drive startup dialogs and menu loading directly. |
| 2026-04-25 | `bash scripts/test.sh` + `bash scripts/build.sh --launch` + in-game inspection | Pass | Added `Prometheus` -> `QA` instruction/result panel backed by `~/Library/Application Support/Timberborn/PrometheusQA`; tests stayed at 26 passing, startup logs showed `Prometheus (v0.2)`, the panel rendered in-game, and a `Passed` result was appended/logged. |
| 2026-04-25 | `bash scripts/test.sh` + `bash scripts/build.sh` | Pass | Added the grid environment sampler/merge layer, moved profile-to-environment policy out of `FireExposureController`, and raised the plain C# suite to 29 passing tests. |
| 2026-04-25 | `bash scripts/test.sh` + `bash scripts/build.sh` | Pass | Added dependency-light terrain column sampling policy for terrain mass vs top-surface cells; plain C# suite is now 30 passing tests. |
| 2026-04-25 | `bash scripts/test.sh` + `bash scripts/build.sh --test` | Pass | Added active-cell heat/smoke/ember emission, exposed-face transfer limits, forest-line spread coverage, and vegetation profiles for common trees and bushes; plain C# suite is now 32 passing tests. |
| 2026-04-25 | CLI autoload + log scan | Blocked | `-settlementName "<settlement>" -saveName "<save>"` reaches Prometheus startup but crashes Timberborn behavior/navigation ticks, including the clean `Prometheus QA` / `beginning` save. Normal UI loading can still work; CLI autostart uses `LoadSceneInstantly(...)` while menu loading uses `LoadScene(...)`. |
| 2026-04-25 | `cliclick` menu automation | Superseded | The old coordinate automation proved the normal menu path once, but it is no longer the QA driver. Use Computer Use for startup dialogs, main menu loading, and in-game clicks. |
| 2026-04-25 | `bash scripts/build.sh --qa` + manual Prometheus QA panel | Superseded | Loaded `Prometheus QA`, opened `Prometheus` -> `QA`, confirmed the instruction/result buttons rendered, clicked `Passed`, and saw `event=qa_result_recorded result=passed` in `Fire.log`. Repeat this with Computer Use rather than `cliclick`. |
| 2026-04-25 | `cliclick` + `screencapture` tight QA loop | Superseded | Replaced by Computer Use screenshots and clicks. |
| 2026-04-25 | `bash scripts/test.sh` + `bash scripts/build.sh --qa` | Superseded | The live QA instruction file targeted forest-spread/grid validation. `--qa` now runs tests, deploys, clears logs, launches Timberborn, and exits for Computer Use navigation. |
| 2026-04-26 | `bash scripts/test.sh` + `bash scripts/build.sh --qa` + startup log scan | Pass | Reworked spread into a field-first resource model: grid transfer carries heat/ember/smoke only, entities own stochastic ignition, moisture evaporation, fuel depletion, tree death at 25% fuel loss, and burned-out char at zero fuel. Plain C# suite is now 36 passing tests and startup logs showed Prometheus loaded with no scanned exceptions. |
| 2026-04-26 | `bash scripts/build.sh --qa` + `cliclick` ignite pass + log scan | Pass | Live Pine ignition consumed moisture, crossed the 25% tree-death threshold, reached zero fuel, extinguished as burned out, and left a charred remnant. A follow-up build reduced high-speed `burning_tick` telemetry to 16 rows for the burn with no scanned Player.log errors. |
| 2026-04-26 | `git tag pre-phase-3-stabilization` | Pass | Tagged the pre-stabilization baseline before Phase 2 sprint orchestration began. |
| 2026-04-26 | Stabilization board integration | Pass | Integrated P2S-001 through P2S-008 plus P2S-010, P2S-011, and P2S-012. This added the architecture map, split grid/debug/test surfaces, centralized component-cache and reflection probes, named grid policies, source attribution, and the grid simulation coordinator. |
| 2026-04-26 | `git diff --check` + `bash scripts/test.sh` | Pass | Latest integrated stabilization check after P2S-008 passed with 53 plain C# tests. |
| 2026-04-26 | P2S-009 candidate: `git diff --check`, `bash scripts/test.sh`, `bash scripts/build.sh --qa`, startup log scan | Blocked | Candidate branch `codex/P2S-009-add-reset-registry` at `2576cf1` now passes diff check and 56 tests after merging current `main`. It adds reset hook failure isolation, stale Unity-reference pruning, visual reset null-safety, and deferred per-entity reset hook registration until `Awake`, but live QA is still blocked before reset can be exercised. |
| 2026-04-26 | Stabilization backlog review | Pass | Added [stabilization backlog](ARCHIVE/stabilization-sprint-2026-04/BACKLOG.md) for ticket-sized follow-ups not yet represented by board files: runtime visual projection migration, grid telemetry bounds evidence, burst attribution injection, terrain/top-surface aftermath eligibility, and Phase 2 exit evidence matrix. |
| 2026-04-26 | P2S-009 live QA attempts | Blocked | Candidate branch reached persistent `LOADING` after the QA save load started, with logs stopping after `Good group Juice has no goods`. Current `main` reached the main menu after `--qa`, proving the readiness gate is startup-only and does not prove save-load completion. Manual `cliclick` and Computer Use input did not activate the visible `Continue` button in the observed run. |
| 2026-04-26 | `bash scripts/test.sh` + `bash scripts/build.sh --launch` + manual Continue path | Pass | Fixed the current `main` QA-save load lock by removing the global grid `IUpdatableSingleton` and stepping the shared grid coordinator from awakened fire-profiled entities instead. Manual `Return`, `Return`, `Continue`, `Yes` loaded `Prometheus QA - 2026-04-26 18h52m, Day 3-2.autosave` into the settlement; `Player.log` showed `Load time: 11776ms` plus native visual resolution logs and no Prometheus exception. |
| 2026-04-27 | `bash -n scripts/build.sh` + `bash scripts/build.sh --help` | Pass | Removed the old QA readiness wait and `cliclick` menu automation from `scripts/build.sh`. `--qa` now runs tests, deploys, clears logs, launches Timberborn, and exits for Computer Use navigation. |
| 2026-04-27 | P2S-009: `git diff --check` + `bash scripts/test.sh` + `bash scripts/build.sh --qa` + Computer Use reset QA | Pass | Reset registry is integrated. The QA save loaded through Computer Use with `Load time: 12000ms`; `Reset Fire State` reported 989 entities and logs recorded `runtime_reset_registry_started`, `runtime_reset_registry_completed failures=0`, and `debug_reset_fire_exposure result=success`. |
| 2026-04-27 | P2S-013: `git diff --check` + `bash scripts/test.sh` + `bash scripts/build.sh --qa` + Computer Use startup | Pass | Configured `FireProfileSpec` source fields now inject heat, embers, and smoke into the grid with `ConfiguredSource:<entityId>` attribution and conservative `RequiresOperation` gating. Plain C# suite is now 60 passing tests. Computer Use reached the main menu; startup logs showed Prometheus loaded with no scanned Prometheus errors. No live `grid_source_*` rows appeared during menu startup because the currently authored deployed profiles do not emit sources before a save is loaded. |
| 2026-04-27 | P2S-013 integration on `main`: `git diff --check` + `bash scripts/test.sh` + `bash scripts/build.sh --qa` + Computer Use startup | Pass | Configured source injection is integrated with P2S-014 environment sampling, P2S-015 runtime projection, and P2S-016 visual catalog changes. Plain C# suite is now 69 passing tests. Computer Use reached the main menu; startup logs showed Prometheus loaded and `environment=deferred:terrain/block/water/soil_runtime_probe` with no scanned Prometheus errors. |
| 2026-04-27 | P2S-017 integration on `main`: `git diff --check` + `bash scripts/test.sh` + `bash scripts/build.sh --qa` + Computer Use startup | Pass | Effect appliers now route direct/cached component assumptions through the integration facade, reset registry entity discovery uses the same facade lookup, and the beaver clear path no-ops when the reset setter API is missing instead of applying a compensating mutation. Plain C# suite is 74 passing tests. Computer Use reached the main menu; startup logs showed Prometheus loaded and the compatibility summary with no scanned Prometheus exceptions. |
| 2026-04-27 | P2S-022 integration on `main`: `git diff --check` + `bash scripts/test.sh` | Pass | Added Fertile Ash field amendment runtime state keyed by fire-grid coordinate with duration, charges, consume, expiry, reset, and debug count. Plain C# suite is now 78 passing tests. |
| 2026-04-27 | P2S-019 integration on `main`: `git diff --check` + `bash scripts/test.sh` + `bash scripts/build.sh` | Pass | Native gatherable discovery found no authored ash natural-resource template, so the safe wrapper uses Timberborn recovered-good stacks and validates `IGoodService.HasGood("FertileAsh")` before queueing. Plain C# suite is now 81 passing tests. Live collection proof remains for P2S-020/P2S-021. |
| 2026-04-27 | P2S-023 integration on `main`: `git diff --check` + `bash scripts/test.sh` + `bash scripts/build.sh --qa` + Computer Use save load + startup log scan | Pass | Fertile Ash field amendments now reduce eligible crop `Growable.GrowthTimeInDays` by 10%, exclude trees and bushes through component classification, tick amendment expiry from a runtime singleton, and restore base growth time on expiry/reset. Plain C# suite is now 83 passing tests. Computer Use loaded `Prometheus QA - 2026-04-26 23h01m, Day 3-2.autosave`; logs showed `Load time: 11999ms` with Prometheus loaded and no scanned Prometheus errors. |
| 2026-04-27 | P2S-020 integration on `main`: `git diff --check`, `bash scripts/test.sh`, `bash scripts/build.sh --qa`, Computer Use live QA | Pass | Valid charred Pine aftermath queues visible native recovered-good stacks with `FertileAsh`: `fertile_ash_recovered_good_stack_queued` and `fertile_ash_spawn_queued` were captured at coordinates `49,3,7` and `23,4,11`. Computer Use confirmed a visible Rubble stack containing `Fertile ash 1`, and the District Center inventory showed `Fertile ash 7` after beavers picked up ash. A repeated soil-moisture sample warning is now de-duplicated so live QA remains observable. Plain C# suite is now 85 passing tests; final main `--qa` reached the main menu after deploy. |
| 2026-04-27 | P2S-021 candidate: `git diff --check` + `bash scripts/test.sh` + `bash scripts/build.sh --qa` + Computer Use reset QA | Pass | Added Fertile Ash recovered-good queue telemetry state, source attribution on ash queue logs, debug selection queue counts, and an ash reset hook. Computer Use loaded `Prometheus QA - 2026-04-27 02h37m, Day 4-3.autosave` and clicked `Reset Fire State`; logs recorded `fertile_ash_reset_state queuedStacks=0 queuedAmount=0 source=none sourceKind=none damageCategory=none nativeStacksDestroyed=0 reason=native_recovered_good_stack_owned_by_timberborn`, `runtime_reset_registry_completed failures=0`, and `debug_reset_fire_exposure result=success`. |
| 2026-04-27 | P2S-024 integration on `main`: `git diff --check` + `bash scripts/test.sh` | Pass | Farmhouse ash application discovery chose a farmhouse-scoped `WorkplaceBehavior` decorator, native `PlantingSpotFinder.FindClosest(Vector3)` reflection adapter, direct district-inventory `FertileAsh` consumption after target selection, and `FireFieldAmendmentRuntimeState.SetAmendment(...)` as the P2S-025 path. Plain C# suite was 86 passing tests after integration. |
| 2026-04-27 | P2S-025 worker and orchestrator checks: `bash scripts/test.sh` + `bash scripts/build.sh --qa` + copied fixture load attempts | Blocked | The discarded farmhouse amendment prototype passed 90 tests and `--qa` built/deployed, but it was not kept as a carry-forward branch. The copied `Prometheus P2S-025 QA` fixture has farmhouse, carrots, and injected District Center `FertileAsh`, but loading it through the known mod-version mismatch warning crashed during `Timberborn.DwellingSystem.Dweller.Load`. Removing stale `Dweller` components with `Home: null` avoided that crash but left the save hung after `ComponentCache._components` resolution, with no `Load time` or amendment telemetry. |
| 2026-04-27 | P2S-026 integration on `main`: `git diff --check` + `bash scripts/test.sh` | Pass | Added repo guardrails for internal Markdown under `Assets/Mods/Prometheus`, dependency-light compile-item sync in `Prometheus.Tests.csproj`, and QA-facing telemetry token stability/registration. Plain C# suite is now 89 passing tests. |
| 2026-04-27 | P2S-027 closeout: `git diff --check` + `bash scripts/test.sh` + `bash scripts/build.sh --qa` + startup log scan | Pass | Closeout validation passed with 89 plain C# tests. `--qa` deployed the current branch, cleared logs, and launched Timberborn. `Player.log` showed `- Prometheus (v0.2)` and `Prometheus loaded: fire system foundation initialized.`; `Fire.log` recorded the compatibility summary. No scanned Prometheus exceptions were present. |

## Durable Context

- Phase 1 live QA previously validated ignition, spread, extinguish, damage, dead/ash terminal behavior, and `Reset Fire Sim` clean-slate recovery.
- The Prometheus debug UI uses TimberUi and Moddable Tool Groups through `Prometheus` -> `Actions`, `Visuals`, `Selection`, `QA`, and `Log`.
- The `QA` panel reads live instructions from `~/Library/Application Support/Timberborn/PrometheusQA/instructions.md` and appends `Passed` / `Failed` / `Blocked` results to `~/Library/Application Support/Timberborn/PrometheusQA/results.md`.
- Timberborn can autoload saves from the command line with `-settlementName "<settlement>" -saveName "<save without .timber>"`; experimental saves are used when the game is in experimental mode. Treat this as unsafe for live QA on the current mod stack because autostart uses `LoadSceneInstantly(...)` rather than the normal menu `LoadScene(...)` path.
- `bash scripts/build.sh --qa` runs tests, deploys, clears logs, launches Timberborn, then exits. It does not wait for Prometheus startup, menu state, or save-load completion.
- `scripts/build.sh` uses a shared build/QA lock under `~/Library/Application Support/Timberborn/PrometheusQA/locks` so sibling ticket worktrees wait instead of deploying, clearing logs, stopping Timberborn, or launching QA at the same time. `--qa` keeps the lock after launch until `bash scripts/build.sh --release-qa-lock` is run.
- [ORCHESTRATION.md](ORCHESTRATION.md) owns the durable kickoff prompt, startup checklist, run loop, and lock rules for future multi-agent orchestration runs.
- Normal menu loading is currently viable again on `main`: on 2026-04-26, Computer Use drove `Return`, `Return`, `Continue`, `Yes` and loaded the latest `Prometheus QA` autosave into the settlement.
- Current verified Prometheus toolbar coordinates at 1920x1080 can drift by active Timberborn tool groups; prefer screenshot-confirmed clicks before recording live QA evidence.
- Use Computer Use for in-game QA clicks, screenshots, and menu evidence.
- The visual authoring tool remains available for `Smoke`, `Ash`, `Steam`, `Fire`, `Sparks`, and `Char`, including selected-entity temporary preview and JSON/log export.
- `Reset Fire State` is now backed by `FireResetRegistry`: global runtime-state hooks are registered once, while entity reset hooks are discovered from loaded ComponentCache entries only when the reset command runs. Do not hold singleton delegates to transient entity components.
- `scripts/build.sh` rewrites external build-project Prometheus compile items to point at the active worktree source, avoiding stale sibling-project DLLs during ticket worktree validation.
- Prometheus runtime decorators must not do expensive Timberborn component-cache scans or scene/resource binding during save deserialization. Use `PrometheusWorldLoadState.WorldReady` and run that work after Timberborn `IPostLoadableSingleton.PostLoad()` emits `world_load_state_changed ready=true stage=post_load`.
- Old bucket-kit, firefighting-foam, fire-control-gear, fireworks-crate, and festival-risk scaffolding has been pruned from active content; Fertile Ash remains the core post-fire resource direction.
- Fertile Ash currently has valid-charred-source recovered-good spawning, visible recovered stack proof, normal District Center storage proof, reset telemetry for Prometheus-owned ash queue evidence, field-amendment runtime state, and a dependency-light crop growth effect. Farmhouse/farmer application is deferred under TKT-001; redo it later from the P2S-024 discovery notes after a fresh live fixture can prove ash consumption and live farmhouse-applied crop-growth behavior.
- Phase 3 controlled burns must be proven through preparation and containment, not a new controlled-burn zone, permit, source type, building, schedule, or labor path.

Source of truth: current UI labels and telemetry event names should be checked in source rather than copied here.

## Open Blockers

| Blocker | Status | Next Check |
| --- | --- | --- |
| Sparse 3D grid needs propagation/profile validation | Active | Configured source injection is dependency-light verified and startup-clean. Save-load now reaches post-load; a future live slice should load a save with an authored emitting source and capture `grid_source_injected` plus downstream ignition evidence. |
| Fertile Ash farmhouse application live QA | Blocked | Provide or create a fresh loadable fixture with a finished farmhouse, eligible crop planting spots, and stored `FertileAsh`. The copied `Prometheus P2S-025 QA` fixture is not viable for acceptance evidence; do not integrate P2S-025 until `fertile_ash_farmhouse_amendment_applied`, decreased stored ash, and faster amended crop growth than a nearby control crop are captured. |
| CLI autoload crashes saves after Prometheus startup | Mitigated | Use normal menu loading for live QA. `--qa` launches Timberborn and then hands navigation to Computer Use. |
| Runtime visuals need prepared-burn QA evidence | Active | Dependency-light TKT-006 visual rules now project ember pressure, smoke, fire, steam, char, and desiccation from existing exposure/damage state. Run live prepared-burn QA after TKT-003/TKT-005 provide the repeatable ignition and containment fixture, then capture screenshots or video plus `Player.log` / `Fire.log` scans. |
| Phase 3 containment proof is not yet live-validated | Active | TKT-005 dependency-light rules now prove prepared containment inputs bound spread more than comparable controls. Use live QA to capture a prepared burn that stays bounded and an unprepared/control burn that spreads more aggressively after TKT-003/TKT-004 are ready enough. |
| Timberborn menu automation map is missing | Active | Create a screenshot-backed map of main menu, Escape menu, in-game toolbar groups, Prometheus group entries, and keyboard controls before assigning more UI-heavy QA work to agents. |
| Explosion request/apply policy needs broader re-validation | Carryover | Use [VALIDATION/explosion-policy.md](VALIDATION/explosion-policy.md) if gaps reappear. |
| Worker/building exposure needs Phase 2 live validation | Carryover | Validate after the grid model stabilizes. |
| Unity asset import workflow is still manual | Carryover | Document or script after Unity license/import path is stable. |

## Next Exact Action

Continue Phase 3 live QA after the fresh `bash scripts/build.sh --test --launch`. Retest a single live pine burn first: expected visual progression is native model-state driven, with `#Dying`, `#Dead`, and `#Leftover` visible; flame stops at dead, smoke remains at dead, and stump smoke fades quickly. The current char-color experiment uses tree material `_Grayscale` plus aggressive `_LightingStrength` reduction, including negative values for dead and stump stages; inspect `Fire.log` for `visual_surface_material_char_applied` rows after the tree changes stage. If the tree pass looks right, use Stop Fires or a clean save state before crop QA so tree-fire activity does not contaminate the carrot test. Then retest the same contiguous carrot block and inspect `Fire.log` for `grid_runtime_state`, `visual_runtime_intensity`, and carrot `ignited` rows.

Use [tickets/README.md](tickets/README.md) for ticket assignment and status moves. Documentation-only ticket updates do not require runtime verification; keep claims tied to the right source of truth and run heavier checks only when behavior, scripts, content, or code changes.

## Resume Checklist

- [ ] Run `bash scripts/test.sh`.
- [ ] Run `bash scripts/build.sh --launch` for in-game QA loops.
- [ ] Use `bash scripts/build.sh --qa` when the next step benefits from tests, deployment, cleared logs, and a fresh Timberborn launch before Computer Use navigation.
- [ ] Use normal menu loading for live QA; CLI `-settlementName` / `-saveName` currently crashes after Prometheus startup.
- [ ] Open `Prometheus` -> `QA`; confirm the current instruction appears and result buttons are visible.
- [ ] Open `Prometheus` -> `Visuals`; confirm Timberborn object selection still works while the panel is open.
- [ ] Select a Bakery, platform, tree, and berry bush; confirm the Visuals target summary and JSON target kind are readable.
- [ ] Apply one particle effect and the full preset, then `Clear Preview`; confirm particles and material overrides are removed.
- [ ] Trigger `Reset Fire State`; confirm registry telemetry completes with `failures=0`.
- [ ] Record any new verified behavior here before ending meaningful work.

## References

| Need | Source |
| --- | --- |
| Build/deploy details | `bash scripts/build.sh --help` |
| Validation gates | [TEST_PLAN.md](TEST_PLAN.md) |
| Durable design | [DESIGN.md](DESIGN.md) |
| Repo map | [INDEX.md](INDEX.md) |
| Ticket board | [tickets/README.md](tickets/README.md) |
